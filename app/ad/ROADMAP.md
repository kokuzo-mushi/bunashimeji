# Shimeji Neo — 開発ロードマップ

最終更新: 2026-01-10 (Asia/Tokyo)

この文書は、Shimeji Neo プロジェクトの開発ロードマップとフェーズ管理について定義します。

---

## 1. 現在の進捗

- **現在のフェーズ**: **Phase 5: 次世代アーキテクチャへの刷新 (Next-Gen Architecture)** - 準備中
- **完了済みタスク**:
  - Phase 1〜4 の全工程（機能拡張、物理演算強化、インストーラ作成まで）
  - セキュリティガイドライン (`SECURITY_IMPLEMENTATION_GUIDE.md`) の策定
 - **次のステップ**: GUIフレームワークの刷新 (Compose)、ネイティブ連携の完全移行 (Panama)、残存アクションの実装

---

## 2. 開発ロードマップ

開発プロジェクトを以下の4つのフェーズに分割して進める。

### Phase 1: 基盤の安定化と回復 (Foundation & Recovery) - **完了**
- **目的**: `EventDispatcher` のリファクタリングによって生じた技術的負債を返済し、プロジェクトのコア機能をテスト可能な健全な状態に戻す。
- **主要タスク**:
  - `EventDispatcher` に、イベントを受け取ってトリガーを評価・発火させるメソッドを実装する。
  - `Mockito` を導入し、API変更に伴い無効化されたテスト群を再設計・有効化する。
- **テストのタイミング**:
  - `EventDispatcher`の評価メソッド実装後、直ちにユニットテストを作成・実行する。
  - `Mockito`導入後、無効化されていたテストを一つずつ有効化し、その都度テストを実行してリグレッションを防ぐ。
- **完了条件**: イベント評価のコアロジックが実装され、その正しさがユニットテストによって継続的に保証される状態になる。

### Phase 2: 行動定義の実装 (Behavior Implementation) - **完了**
- **目的**: マスコットが「いつ、何をするか」という振る舞いをXMLファイルから動的に読み込み、実行するための仕組みを構築する。
- **主要タスク**:
  - `Action`（行動）と `Behavior`（振る舞い）のインターフェースとクラス構造を設計・実装する。
  - XML等の外部ファイルから `Behavior` 定義を読み込むパーサーを実装する。
  - `Mascot` クラスが `Action` を実行し、自身の状態を更新するロジックを実装する。
- **テストのタイミング**:
  - `Action`の各実装クラス（`WalkAction`など）に対して、個別のユニットテストを作成する。
  - XMLパーサー実装後、定義ファイルを正しく読み込み、`Behavior`オブジェクトが生成されることをテストする。
  - `Mascot`が`Action`を実行し、期待通りに状態（座標など）が変化するかを検証する統合テストを実施する。
- **完了条件**: 「特定の条件が満たされたら、定義されたアクションを実行する」という、マスコットの基本的な行動サイクルが完成する。

### Phase 3: アプリケーションとしての統合 (Application Integration) - **完了**
- **目的**: コアコンポーネントを統合し、ユーザーが操作できるデスクトップアプリケーションとして完成させる。
- **主要タスク**:
  - [x] アプリケーションの起動・終了処理、システムトレイメニュー等のUIを実装する。
  - [x] マスコットのアニメーションを描画するビュー（ウィンドウ）を実装する。
  - [x] 設定ファイル（`config.xml`等）の読み込みと適用ロジックを実装する。
- **テストのタイミング**:
  - このフェーズでは、ユニットテストに加え、**手動での受け入れテスト（E2Eテスト）**が重要になる。
  - アプリケーションを実際に起動し、マスコットが表示され、システムトレイが機能することを確認する。
  - 設定ファイルを変更し、アプリケーションの挙動が反映されることを確認する。
- **完了条件**: ユーザーがアプリケーションを起動し、デスクトップ上を動くマスコットと基本的な対話ができる状態になる。

### Phase 4: 機能拡張と最適化 (Feature Enhancement & Optimization) - **完了**
- **目的**: より高度な機能を追加し、パフォーマンスを改善して製品としての完成度を高める。また、技術監査に基づきセキュリティと基盤の刷新を行う。
- **主要タスク**:
  - [x] **ウィンドウ投擲機能 (ThrowAction)**:
    - ターゲット認識、持ち上げ、運搬、投擲（放物線）、勝利ポーズ、自動復帰（アニメーション付き）の実装完了。
    - 最小化ウィンドウの復元や壁判定の除外など、エッジケースへの対応も完了。
  - [x] **物理演算の強化**:
    - マウスで投げた際の壁・床でのバウンド処理を追加。
  - [x] **UI/UX改善**:
    - マスコット選択画面でのスキンプレビュー機能。
    - クリックによる選択確定。
  - [x] **多体連携アクション (基本)**:
    - マスコット同士が近づいた際の挨拶 (`Greet`) アクションの実装。
    - 相手の方向を向く (`LookAction`) 制御の実装。
  - [x] **壁・天井の移動と遷移 (Wall-Ceiling Transition)**:
    - **Kinematic Corner Transition**: 物理演算を一時的に無効化し、幾何学的な軌道計算 (`CornerMath`) で壁と天井を滑らかに移動するロジックの実装。
    - **Ceiling Actions**: 天井移動 (`CeilingCrawl`)、天井待機 (`CeilingStay`)、天井からの降下 (`CornerTurnDown`) の実装。
    - **Wall Top Actions**: 壁の頂上でのランダム挙動 (`WallTopRandomMove`) としがみつき (`WallTopCling`) の実装。
  - [x] **セキュリティ強化 (Security Hardening)**:
    - `SECURITY_IMPLEMENTATION_GUIDE.md` の策定完了。（実装適用は継続対応）
  - [x] **設定GUIの拡張**:
    - アクションの頻度や挙動を動的に変更できる設定画面の拡充。
  - [x] **Java 21 モダン機能の適用**:
    - **Virtual Threads**: マスコット思考ロジックの仮想スレッド化（Project Loom）。
    - **Records/Switch式**: データクラスと分岐ロジックの書き換えによる安全性向上。
  - [x] **パフォーマンス最適化**:
    - キャッシュ統計の精度向上や、パフォーマンスプロファイリングに基づく最適化。
- **テストのタイミング**:
  - パフォーマンス最適化の前後にベンチマークテストを実施し、改善効果を数値で測定する。
  - 新しい`Behavior`を追加する都度、その動作を確認する。
- **完了条件**: 現代的なデスクトップマスコットとして十分な機能と安定性、および安全性を備える。

### Phase 5: 次世代アーキテクチャへの刷新 (Next-Gen Architecture) - **進行中**
- **目的**: 技術監査報告書に基づき、GUIおよびネイティブ連携基盤を根本的に刷新し、クロスプラットフォーム対応と描画品質を向上させる。
- **主要タスク**:
  - [ ] **GUIフレームワークの移行**:
    - [x] Compose 導入の概念実証 (PoC) の実施 (描画, アニメーション, 透過ウィンドウ)。
    - [x] マスコットロジック (MascotManager) の分離と Compose への統合。
    - [x] 設定画面 (Settings UI) の Compose 移植と物理演算パラメータのリアルタイム反映。
    - [ ] Swing/AWT から **JetBrains Compose Multiplatform** への完全移行。
  - [ ] **ネイティブ連携の刷新**:
    - [x] コアウィンドウ操作 (`NativeWindowUtil`) の Project Panama (FFM API) 化。
    - [ ] JNA 依存の完全排除。
  - [ ] **拡張性の向上**:
    - 設定ファイルの YAML 化、およびスクリプト言語 (Lua/Kotlin) の導入。
  - [ ] **未実装アクションの追加**:
    - **Pull Up**: マスコットがウィンドウの下からよじ登ってくるアクション。
    - **Teeter**: ウィンドウの端でバランスをとるアクション。
- **完了条件**: Windows/macOS/Linux で動作し、高解像度環境でも美しい描画を実現する次世代プラットフォームとなる。